jwt:
  secret: ${JWT_SECRET:change-me-use-a-long-random-secret-value}

spring:
  cloud:
    gateway:
      default-filters:
        - AddRequestHeader=X-Gateway-Secret, ${GATEWAY_INTERNAL_SECRET:change-me}
